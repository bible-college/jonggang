// src/factory/abstract/AbstractActionFactory.js

const AbstractFeatureFactory = require('../../core/AbstractFeatureFactory');

class AbstractActionFactory extends AbstractFeatureFactory {
  // 필요한 경우 공통 유틸이나 액션용 기본 기능 정의 가능
}

module.exports = AbstractActionFactory;
