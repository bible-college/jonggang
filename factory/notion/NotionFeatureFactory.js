// src/factory/notion/NotionFeatureFactory.js

const AbstractActionFactory = require('../abstract/AbstractActionFactory');

class NotionFeatureFactory extends AbstractActionFactory {
  // Notion 전용 기능이 있을 경우 여기에 공통 메서드 정의 가능
  // 예: Notion 인증 처리, 공통 속성 매핑 등
}

module.exports = NotionFeatureFactory;
